# F<核心技術編號>-<流水號> <功能名稱>

<!-- 本範本複製到 features/ 後使用，文中相對連結以 features/ 為基準。
     文件描述功能的現況：需求變更或新增驗收標準時直接修改本文件，尚未實作的 FR／AC 標上〔規劃中〕。
     狀態為「待規劃」或「待實作」的新功能，整份文件都視為規劃中，不必逐條標記。
     既有功能修改時狀態同樣退回「待規劃」，但只有標上〔規劃中〕的條目尚未實作，其餘仍是現況。 -->

| 欄位 | 內容 |
| :--- | :--- |
| ID | F?-?? |
| 核心技術 | ? — <名稱>（見 [專案總覽](../README.md#核心技術)） |
| 狀態 | 待規劃 / 待實作 / 實作中 / ✅ 已完成 |
| 依賴 | 需要先完成的功能 ID，沒有就寫「無」 |
| 程式碼 | 進入點與模組路徑，還沒有就寫「尚未實作」 |

## 1. 背景與目標

這個功能要解決什麼問題？完成後使用者能得到什麼？

## 2. 使用情境

- 身為 <角色>，我想要 <做某件事>，以便 <得到某個好處>。

## 3. 範圍

**範圍內**

- …

**範圍外**（實作時不要做）

每一項註明去處：屬於其他功能就寫功能 ID，之後要做的小改善就記到 [TODO.md](../../TODO.md)。

- …

## 4. 功能需求

每條需求要能被至少一條驗收標準涵蓋。

- **FR-1**：…
- **FR-2**〔規劃中〕：…

## 5. 設計

只寫程式碼講不出來的事：流程、規則與其理由、外部系統的限制、資料契約、模組佈局、CLI。需求與驗收標準可以直接連到這裡的小節，例如 `[§5.2](#52-...)`。

- **輸入與輸出**：讀哪些資料、來自哪個功能，產出什麼、給哪個功能用，並寫明格式、存放位置與欄位定義。其他功能已經定義的契約，連過去就好，不要複製內容。
- **缺值處理**：欄位缺失時的表現（依 [conventions.md](../conventions.md#防禦性設計)，缺值為 `null`，不回填）。

### 5.1 <小節>

…

## 6. 驗收標準

每條都要寫出**可實際執行**的驗證方式與明確的通過條件，不寫「運作正常」這類無法判定的描述。

- 驗證方式優先寫成 pytest 指令，例如 `uv run pytest tests/test_x.py -k <函式名>`。測試程式與實作一起撰寫，不要把腳本貼在功能文件裡（測試慣例見 [conventions.md](../conventions.md#測試)）。
- 「輸入 → 預期結果」可以寫成表格留在功能文件，測試再用 `parametrize` 實作。
- 需要網路或外部服務的條目，標題標註 `〔需網路〕`，測試放在 `tests/e2e/` 並加上 `@pytest.mark.network`，驗證方式寫 `uv run pytest -m network tests/e2e/...`。
- 只能由人判斷的部分（例如 AI 的理由是否合理），在通過條件中明確寫出由誰判斷、看什麼。

### AC-1：<標題>（涵蓋 FR-1）

- **Given**：前置條件
- **When**：執行的動作
- **Then**：預期結果
- **驗證方式**：`uv run pytest tests/test_<模組>.py -k <函式名>`
- **通過條件**：全部 passed。

## 7. 待決問題

進入實作前必須清空；每條註明需要誰決定。

- [ ] …
